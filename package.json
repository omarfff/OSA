{
  "name": "osa-agent-trust-oracle",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "OSA Agent Trust Oracle: rank and verify agent/API endpoints before purchase.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "test:python": "python3 -m unittest discover -s examples/python_llm_gateway -p 'test_*.py'",
    "chaos": "node scripts/chaos.mjs",
    "contract:compile": "node scripts/arbitrum-compile.mjs",
    "arbitrum:deploy": "node scripts/arbitrum-deploy.mjs",
    "arbitrum:record": "node scripts/arbitrum-record.mjs",
    "lead:audit": "node tools/lead-leakage-audit.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@x402/core": "^2.10.0",
    "@x402/evm": "^2.10.0",
    "@x402/express": "^2.10.0",
    "@x402/extensions": "^2.10.0",
    "express": "^5.1.0",
    "viem": "2.55.19"
  },
  "devDependencies": {
    "solc": "0.8.36"
  }
}
